# 🧠 3 Sum Problem [(LeetCode 15)](https://leetcode.com/problems/3sum/description/)

---

## 📌 Problem Statement

Given an integer array `nums`, return **all unique triplets** `[nums[i], nums[j], nums[k]]` such that:

```
i != j
i != k
j != k
nums[i] + nums[j] + nums[k] == 0
```

The solution set must **not contain duplicate triplets**.

---

# 🧠 Pattern Recognition

This belongs to:

* Two Pointer Pattern
* Sorting + Fix One Element
* K-Sum Problems

---

# 🔁 Similar Problems (Same Pattern)

1. 2Sum
2. 4Sum
3. 3Sum Closest
4. Count Triplets with Given Sum
5. K-Sum Generalization

---

## 🎯 Core Challenge

1. Find triplets whose sum = 0
2. Avoid duplicate triplets
3. Optimize from brute force O(n³)

---

# 🔴 1. Brute Force Approach

## 💡 Idea

Try **all possible triplets** using 3 nested loops.

### Steps

* Fix `i`
* Fix `j > i`
* Fix `k > j`
* Check if sum == 0
* Store unique triplets (use sorting + Set)

---

## ⏱ Time Complexity

```
O(n³)
```

## 📦 Space Complexity

```
O(1) or O(n) depending on storage
```

---

## 💻 Java Code (Brute Force)

```java
import java.util.*;

class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        Set<List<Integer>> set = new HashSet<>();
        int n = nums.length;

        for(int i = 0; i < n; i++){
            for(int j = i+1; j < n; j++){
                for(int k = j+1; k < n; k++){
                    if(nums[i] + nums[j] + nums[k] == 0){
                        List<Integer> temp = Arrays.asList(nums[i], nums[j], nums[k]);
                        Collections.sort(temp);
                        set.add(temp);
                    }
                }
            }
        }

        return new ArrayList<>(set);
    }
}
```

---

## ❌ Why Not Acceptable?

* Fails for large constraints (n up to 3000)
* Too slow → O(n³)

---

# 🟡 2. Better Approach (Hashing Based)

## 💡 Idea

Fix one element, then reduce problem to **2Sum** using HashSet.

For each `i`:

* Create a HashSet
* For every `j > i`
* Check if `-(nums[i] + nums[j])` exists

---

## ⏱ Time Complexity

```
O(n²)
```

## 📦 Space Complexity

```
O(n)
```

---

## 💻 Java Code (Better)

```java
import java.util.*;

class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        Set<List<Integer>> result = new HashSet<>();
        int n = nums.length;

        for(int i = 0; i < n; i++){
            Set<Integer> set = new HashSet<>();

            for(int j = i+1; j < n; j++){
                int third = -(nums[i] + nums[j]);

                if(set.contains(third)){
                    List<Integer> temp = Arrays.asList(nums[i], nums[j], third);
                    Collections.sort(temp);
                    result.add(temp);
                }

                set.add(nums[j]);
            }
        }

        return new ArrayList<>(result);
    }
}
```

---

## ⚠️ Limitation

* Extra space used
* Duplicate handling still tricky
* Sorting inside loop

---

# 🟢 3. Optimal Approach (Two Pointer Technique)

## 💡 Core Insight

If array is sorted:

* We can fix one element
* Use two pointers to find remaining two

This removes:

* Need for hashing
* Need for sorting each triplet
* Duplicate complexity reduces

---

## 🧩 Steps

1. Sort the array
2. Loop `i` from 0 to n-3
3. Skip duplicate `nums[i]`
4. Use two pointers:

   * `left = i + 1`
   * `right = n - 1`
5. If sum:

   * < 0 → move left++
   * > 0 → move right--
   * == 0 → store triplet and skip duplicates

---

## ⏱ Time Complexity

```
O(n²)
```

## 📦 Space Complexity

```
O(1)  (ignoring output)
```

---

## 💻 Java Code (Optimal – Industry Standard)

```java
import java.util.*;

class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        List<List<Integer>> result = new ArrayList<>();
        Arrays.sort(nums);
        int n = nums.length;

        for(int i = 0; i < n-2; i++){

            if(i > 0 && nums[i] == nums[i-1]) 
                continue;

            int left = i + 1;
            int right = n - 1;

            while(left < right){
                int sum = nums[i] + nums[left] + nums[right];

                if(sum < 0){
                    left++;
                }
                else if(sum > 0){
                    right--;
                }
                else{
                    result.add(Arrays.asList(nums[i], nums[left], nums[right]));

                    while(left < right && nums[left] == nums[left+1]) left++;
                    while(left < right && nums[right] == nums[right-1]) right--;

                    left++;
                    right--;
                }
            }
        }

        return result;
    }
}
```

---

# 🔥 Why Two Pointer Works?

Because sorting gives:

```
Small → Large
```

If sum too small → increase left
If sum too big → decrease right

Monotonic movement = No repetition

---




# 🏆 Final Comparison

| Approach | Time  | Space | Recommended |
| -------- | ----- | ----- | ----------- |
| Brute    | O(n³) | O(1)  | ❌           |
| Better   | O(n²) | O(n)  | ⚠️          |
| Optimal  | O(n²) | O(1)  | ✅           |

---

# 📝 Interview Summary

If interviewer asks:

> “Can you do better than O(n³)?”

👉 Sort + Two Pointer

That is the expected solution.


